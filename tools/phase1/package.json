{
  "name": "@patchmesh/phase1-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --import ../../tools/test/process-diagnostics.mjs --test \"*.test.ts\""
  },
  "dependencies": {
    "patchmesh-adapters": "workspace:*",
    "patchmesh-core": "workspace:*",
    "patchmesh-observation": "workspace:*",
    "patchmesh-protocol": "workspace:*",
    "patchmesh-storage": "workspace:*"
  }
}
